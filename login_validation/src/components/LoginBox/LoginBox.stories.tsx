import { LoginBox } from "./LoginBox";

export default {
  title: "LoginBox",
  component: LoginBox,
};

export const Primary = {
  args: {
    variant: "primary",
  },
};

export const Secondary = {
  args: {
    variant: "secondary",
  },
};
